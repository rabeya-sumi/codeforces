/*Rabeya Akter
  Britannia University, Cumilla*/

#include<bits/stdc++.h>
using namespace std;
 
int main()
{
    int t, n, a[200005];
    cin>>t;
    while(t--)
    {
        cin>>n;
        for(int i=0 ; i<n ; i++) cin>>a[i];
        sort(a, a+n);
        if(n==2){
            cout<<a[0]<<' '<<a[1]<<'\n';
            continue;
        }
        int pnt=0;
        int diff = a[1]-a[0];
        for(int i=1 ; i<n-1 ; i++){
            if(a[i+1]-a[i]<diff){
                pnt=i;
                diff = a[i+1]-a[i];
            }
        }
        for(int i=pnt+1 ; i<n ; i++) cout<<a[i]<<' ';
        for(int i=0 ; i<=pnt ; i++){
            cout<<a[i]<<' ';
        }
        cout<<'\n';
    }
 
 
return 0;
}
